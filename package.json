{
	"name": "nekoshare",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"build": "turbo build",
		"docker": "cd docker && docker compose -f compose.yml up -d --remove-orphans",
		"docker:build": "cd docker && docker compose -f compose.yml up -d --build --remove-orphans",
		"docker:down": "cd docker && docker compose -f compose.yml down --remove-orphans",
		"docker:dev": "cd docker && docker compose -f compose.dev.yml up -d --remove-orphans",
		"docker:dev:build": "cd docker && docker compose -f compose.dev.yml up -d --build --remove-orphans",
		"docker:dev:down": "cd docker && docker compose -f compose.dev.yml down --remove-orphans",
		"dev": "turbo dev",
		"dev:client": "turbo dev --filter=desktop --filter=web --filter=admin",
		"dev:desktop": "turbo dev --filter=desktop --filter=server",
		"dev:web": "turbo dev --filter=web --filter=server",
		"dev:admin": "turbo dev --filter=admin",
		"dev:server": "turbo dev --filter=server",
		"lint": "turbo lint",
		"format": "prettier --write \"**/*.{ts,tsx,md}\""
	},
	"devDependencies": {
		"@workspace/eslint-config": "workspace:*",
		"@workspace/typescript-config": "workspace:*",
		"prettier": "^3.5.1",
		"turbo": "^2.5.4",
		"typescript": "5.7.3"
	},
	"packageManager": "pnpm@10.4.1",
	"engines": {
		"node": ">=20"
	},
	"pnpm": {
		"onlyBuiltDependencies": [
			"@tailwindcss/oxide",
			"core-js-pure",
			"esbuild",
			"sharp",
			"unrs-resolver"
		]
	}
}
